{
  "buildCommand": "cd 'math quest' && npm install && npm run build",
  "startCommand": "cd 'math quest' && npm run preview -- --host 0.0.0.0 --port $PORT",
  "framework": "React",
  "port": 5173,
  "enviromentVariables": {
    "NODE_ENV": "production"
  }
}
